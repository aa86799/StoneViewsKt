<resources>

    <!-- Base application theme. -->
<!--    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StoneBaseTheme" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="StoneBaseMaterialTheme" parent="Theme.MaterialComponents.Light.NoActionBar"/>

    <style name="MyBottomSheetDialog" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
<!--    <style name="MyBottomSheetDialog" parent="Widget.Design.BottomSheet.Modal">-->
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="TextStyle1">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <!--对话框的样式-->
    <style name="LoadingDialog">
        <!--对话框背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--边框 -->
        <item name="android:windowFrame">@null</item>
        <!--没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!--背景透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 是否有覆盖 -->
        <item name="android:windowContentOverlay">@null</item>
        <!--背景变暗-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="myProgressBarStyle" >
        　　<item name="android:indeterminateDrawable">@drawable/progressbar_bg</item>
        　　<item name="android:minWidth">46dip</item>
        　　<item name="android:maxWidth">46dip</item>
        　　<item name="android:minHeight">46dip</item>
        　　<item name="android:maxHeight">46dip</item>
    </style>

</resources>
